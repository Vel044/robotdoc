DEVPATH=/devices/platform/axi/1000120000.pcie/1f00200000.usb/xhci-hcd.0/usb1/1-1/1-1.3/1-1.3:1.0/tty/ttyACM0 # sysfs 设备路径；说明它挂在 AXI -> PCIe -> USB/xHCI -> usb1 -> 1-1.3 这个 USB 端口链路下。
DEVNAME=/dev/ttyACM0                              # 用户态最终打开的设备节点；LeRobot 命令里的 follower 端口就是 /dev/ttyACM0。
MAJOR=166                                        # Linux 字符设备主设备号；166 通常对应 ACM tty 设备。
MINOR=0                                          # Linux 字符设备次设备号；这里是第 0 个 ttyACM 设备。
SUBSYSTEM=tty                                    # udev 子系统是 tty，说明这是串口/终端类设备，不是视频设备。
USEC_INITIALIZED=3219383                         # udev 初始化该设备的时间戳，单位微秒；主要用于调试设备出现时序。
ID_BUS=usb                                       # 设备来自 USB 总线。
ID_MODEL=USB_Single_Serial                       # USB 产品名；这是一个 USB 单串口设备。
ID_MODEL_ENC=USB\x20Single\x20Serial             # 产品名的转义形式，_ 写成 \x20。
ID_MODEL_ID=55d3                                 # USB product id；和 lsusb 里的 1a86:55d3 后半部分一致。
ID_SERIAL=1a86_USB_Single_Serial_5A7C119177      # udev 生成的完整设备序列标识；用于区分两块同型号串口板。
ID_SERIAL_SHORT=5A7C119177                       # USB 设备短序列号；这块板子的唯一识别号。
ID_VENDOR=1a86                                   # USB vendor id；QinHeng/WCH 厂商 ID。
ID_VENDOR_ENC=1a86                               # vendor 字段的转义形式。
ID_VENDOR_ID=1a86                                # USB vendor id；和 lsusb 里的 1a86:55d3 前半部分一致。
ID_REVISION=0445                                 # USB 设备版本号 bcdDevice。
ID_TYPE=generic                                  # udev 识别出的通用类型。
ID_USB_MODEL=USB_Single_Serial                   # USB 层产品名，和 ID_MODEL 一致。
ID_USB_MODEL_ENC=USB\x20Single\x20Serial         # USB 层产品名转义形式。
ID_USB_MODEL_ID=55d3                             # USB 层 product id。
ID_USB_SERIAL=1a86_USB_Single_Serial_5A7C119177  # USB 层完整序列标识。
ID_USB_SERIAL_SHORT=5A7C119177                   # USB 层短序列号；udev 规则可用它绑定稳定名字。
ID_USB_VENDOR=1a86                               # USB 层厂商字段。
ID_USB_VENDOR_ENC=1a86                           # USB 层厂商字段转义形式。
ID_USB_VENDOR_ID=1a86                            # USB 层 vendor id。
ID_USB_REVISION=0445                             # USB 层设备版本号。
ID_USB_TYPE=generic                              # USB 层通用设备类型。
ID_USB_INTERFACES=:020201:0a0000:                # USB interface class 列表；02/02/01 是 CDC ACM 通信接口，0a/00/00 是 CDC data 接口。
ID_USB_INTERFACE_NUM=00                          # 当前 ttyACM0 来自该 USB 设备的第 0 号 interface。
ID_USB_DRIVER=cdc_acm                            # 关键字段：内核用 cdc_acm 驱动绑定它，所以生成 /dev/ttyACM0。
ID_USB_CLASS_FROM_DATABASE=Communications        # USB 类数据库识别为通信类设备。
ID_VENDOR_FROM_DATABASE=QinHeng Electronics      # USB ID 数据库识别厂商为 QinHeng/WCH。
ID_PATH=platform-xhci-hcd.0-usb-0:1.3:1.0        # 按物理连接路径生成的稳定路径；表示 xhci-hcd.0 下的 1.3 端口、1.0 interface。
ID_PATH_TAG=platform-xhci-hcd_0-usb-0_1_3_1_0    # ID_PATH 的安全标签形式，udev 用于生成 by-path 名字。
ID_MM_CANDIDATE=1                                # ModemManager 可能扫描它；对 LeRobot 来说不是核心字段。
DEVLINKS=/dev/serial/by-path/platform-xhci-hcd.0-usb-0:1.3:1.0 /dev/serial/by-id/usb-1a86_USB_Single_Serial_5A7C119177-if00 /dev/so101_follower_left # udev 生成的稳定软链接；/dev/so101_follower_left 明确说明这块板被绑定为 follower 舵机串口。
TAGS=:systemd:                                   # systemd/udev 标签。
CURRENT_TAGS=:systemd:                           # 当前仍生效的 systemd/udev 标签。
