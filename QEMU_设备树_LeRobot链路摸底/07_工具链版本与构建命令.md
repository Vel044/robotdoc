# 07 工具链版本与构建命令

## 当前 kernel / 构建事实

| 项 | 版本 / 事实 |
| --- | --- |
| 当前真机 kernel | `6.12.75-v8-16k-TEST-PSELECT6+` |
| 架构 | `aarch64` |
| `/proc/version` GCC | `gcc 13.3.0` |
| `/proc/version` ld/binutils | `GNU ld 2.42` |
| Pi defconfig | `linux/arch/arm64/configs/bcm2712_defconfig` |

## 本地 Mac 可用工具

| 工具 | 当前状态 |
| --- | --- |
| GNU make | `gmake 4.4.1`，macOS 自带 `/usr/bin/make 3.81` 不够。 |
| QEMU | `qemu-system-aarch64 10.2.1`、`qemu-img 10.2.1` |
| dtc | `1.7.2` |
| ARM64 交叉 GCC | `aarch64-linux-gnu-gcc 13.3.0` |
| Rust | `rustc 1.90.0`、`cargo 1.90.0`、`rustfmt 1.8.0`、`clippy 0.1.90` |
| 当前缺口 | `bindgen`、`llvm-*` / `ld.lld`、`pahole` 未找到 |

## 当前源码最低版本线

来自：

```text
linux/scripts/min-tool-version.sh
```

| 工具 | 最低版本 |
| --- | --- |
| `binutils` | `2.25.0` |
| `gcc` | `5.1.0` |
| `llvm` / `libclang` | `13.0.1` |
| `rustc` | `1.78.0` |
| `bindgen` | `0.65.1` |

## 构建命令骨架

Pi 5 对应 defconfig：

```text
linux/arch/arm64/configs/bcm2712_defconfig
```

GCC 交叉编译骨架：

```bash
cd linux
gmake ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- bcm2712_defconfig
gmake ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- Image modules dtbs -j<N>
```

Rust kernel 路线先跑：

```bash
cd linux
gmake ARCH=arm64 LLVM=1 rustavailable
```

要启用 Rust kernel 代码，需要：

```text
CONFIG_RUST=y
```

并且 `rustavailable` 通过。

## LeRobot 关键用户态版本

这里只保留对新 kernel ABI/运行时有直接意义的版本。完整包表在附录。

| 项 | 版本 / 事实 | 对 kernel 的意义 |
| --- | --- | --- |
| Python | `3.10.12` | 新 kernel 要能跑这个 Python 用户态。 |
| conda | `25.9.1` | 提供 Python 包和 native `.so` 动态库路径。 |
| LeRobot | `0.3.4` | `record.py`、SO101、camera、motor、dataset、policy 主项目。 |
| torch | metadata `2.7.1`，import `2.7.1+cpu` | ACT policy CPU 推理。 |
| OpenCV / cv2 | package `4.12.0.88`，import `4.12.0` | 打开 `/dev/video0`、`/dev/video2`。 |
| numpy | `2.2.6` | 图像、状态、动作数据数组。 |
| pyserial | `3.5` | 打开 `/dev/ttyACM0`、`/dev/ttyACM1`。 |
| feetech-servo-sdk | `1.0.0` | 舵机协议封包和同步读写。 |
| datasets / pyarrow / pandas | `4.1.1` / `22.0.0` / `2.3.3` | dataset/parquet/episode 数据组织。 |
| av | `15.1.0` | 视频编码/解码路径。 |
| safetensors | `0.6.2` | ACT 权重和 normalization stats 加载。 |

